# tokens-core is a standalone package nested under a yarn superproject.
# Opt out of the inherited packageManager (yarn) enforcement so pnpm can manage
# this package's own dev toolchain independently.
package-manager-strict=false
